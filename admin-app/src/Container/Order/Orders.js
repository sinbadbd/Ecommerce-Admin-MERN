import React from 'react'
import Layout from '../../Component/Layout'

const  Orders = (props) => {
    return (
        <Layout sidebar>
            Orders
        </Layout>
    )
}

export default Orders